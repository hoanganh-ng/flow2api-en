[global]
api_key = "han1234"
admin_username = "admin"
admin_password = "admin"

[flow]
labs_base_url = "https://labs.google/fx/api"
api_base_url = "https://aisandbox-pa.googleapis.com/v1"
timeout = 120
max_retries = 3
image_request_timeout = 40
image_timeout_retry_count = 1
image_timeout_retry_delay = 0.8
image_timeout_use_media_proxy_fallback = true
image_prefer_media_proxy = false
image_slot_wait_timeout = 480
image_launch_soft_limit = 20
image_launch_wait_timeout = 480
image_launch_stagger_ms = 0
video_slot_wait_timeout = 480
video_launch_soft_limit = 20
video_launch_wait_timeout = 480
video_launch_stagger_ms = 0
poll_interval = 3.0
max_poll_attempts = 200

[server]
host = "0.0.0.0"
port = 8001

[debug]
enabled = false
log_requests = true
log_responses = true
mask_token = true

[proxy]
proxy_enabled = false
proxy_url = ""

[generation]
image_timeout = 300
video_timeout = 1500

[call_logic]
call_mode = "default"  # default=random round-robin strategy, polling=sequential round-robin strategy

[admin]
error_ban_threshold = 3

[cache]
enabled = false
timeout = 7200  # Cache timeout in seconds, default 2 hours; set to 0 to disable automatic deletion
base_url = ""   # Base URL for accessing cached files; leave empty to use the server address

[captcha]
captcha_method = "extension"  # Captcha method: extension/yescaptcha/browser/personal/remote_browser
browser_recaptcha_settle_seconds = 3.0  # Extra settle wait after reload/clr ready
browser_count = 1  # Number of headed browser instances in browser mode
browser_captcha_max_retries = 5  # Max retries per captcha fetch in browser mode
browser_captcha_generation_retries = 6  # Total retries when generation endpoint encounters reCAPTCHA evaluation failed
personal_project_pool_size = 4  # Default project pool size per Token in personal mode (only affects project rotation, not captcha tab count)
personal_max_resident_tabs = 5  # Shared captcha-tab upper limit in personal mode; all Tokens/projects share this pool
browser_personal_fresh_restart_every_n_solves = 10  # Number of successful captchas in personal mode before cleaning and restarting the browser; 0 disables
personal_idle_tab_ttl_seconds = 600  # Tab idle reclaim time in seconds in personal mode
yescaptcha_api_key = ""  # YesCaptcha API key
yescaptcha_base_url = "https://api.yescaptcha.com"
yescaptcha_task_type = "RecaptchaV3TaskProxylessM1S9"  # Options: RecaptchaV3TaskProxyless/RecaptchaV3TaskProxylessM1/RecaptchaV3TaskProxylessM1S7/RecaptchaV3TaskProxylessM1S9
remote_browser_base_url = ""  # Remote headed captcha service address
remote_browser_api_key = ""   # Remote headed captcha service API Key
remote_browser_timeout = 60   # Remote headed captcha request timeout in seconds
capsolver_api_key = ""
capsolver_base_url = "https://api.capsolver.com"
